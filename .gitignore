# actor secret material — NEVER commit (Ed25519 + ML-DSA + KEM private keys)
.kagi/
*identity.edn

# clojure / tooling
.cpcache/
.clj-kondo/.cache/
target/
jvm-mirror-20260923/
.nrepl-port
node_modules/
