#!/usr/bin/env bash
# kagi — 自己主権・対量子(PQC) secrets vault CLI (op 相当)
# PQC は JDK24 標準 provider を使うため bb ではなく clojure(full JDK)で実行する。
# vault は実行ディレクトリの ./.kagi に置かれる。
cd "$(dirname "$0")/.." || exit 1
exec clojure -M:dev:cli "$@"
